-# code: 'UTF-8'

%body
  %div
    %h1= @gameQueue.name
    %h2 Games in this queue
    %ul
    - @games.each do |g|
      %li
        %a{href: "/games/#{g.id}"} #{g.name}
        %span - Current Votes: #{g.current_votes}
        %form{action: "/game_queues/#{@gameQueue.id}/change_game", method: "post"}
          %input{type: 'hidden', name: 'game_id', value: "#{g.id}"}
          %select{name: 'game_queue_id', label: " "}
            - @gameQueues.each do |gq|
              %option{value: "#{gq.id}"} #{gq.name}
          %input{type: 'submit', value: 'Change Game Queue'}
  %a{href: "/game_queues/"} Back to Game Queue Index
  %div
    %a{href: "/"} Back to Main Page
